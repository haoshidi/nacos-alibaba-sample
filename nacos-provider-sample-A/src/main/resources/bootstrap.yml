server:
  port: 8081
spring:
  application:
    name: nacos-provider-sample
  cloud:
    nacos:
      discovery:
        server-addr: http://nacos.chuangzans.com
management:
  endpoints:
    web:
      exposure:
        include: '*'
